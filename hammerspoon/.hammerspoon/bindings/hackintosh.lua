require '../utils'

-- Trackpad Gestures (for Hackintosh)
bind(one, 'up', nil, keyStroke('ctrl', 'up'))
bind(one, 'down', nil, keyStroke('ctrl', 'down'))
bind(one, 'left', nil, keyStroke('ctrl', 'right'))
bind(one, 'right', nil, keyStroke('ctrl', 'left'))
